import * as React from "react";

const Header: React.FunctionComponent = () => {
  return (
    <section className="header">
      <h2 className="brandText">Blog writting</h2>
    </section>
  );
};

export default Header;
